schema_version: "1.0"
# Planning estimates derived from the pinned official repositories; these are
# resource estimates, not performance benchmarks.
modules:
  DSG: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "0.5-1 GB", cpu: "1-2 cores", gpu_vram: "not required", model_size: "spaCy en_core_web_sm 3.7.1; tens of MB", cpu_only: yes, low_resource: high, cluster: high, recommendation: "local CPU at pinned commit"}
  TARGER: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "4-8 GB", cpu: "2-4 cores", gpu_vram: "optional 3-5 GB", model_size: "BiLSTM-CNN-CRF IBM.h5; 1.23 GB", cpu_only: yes, low_resource: "low-medium", cluster: high, recommendation: "server CPU/GPU; public endpoint for development"}
  DSS: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "4-8 GB", cpu: "4+ cores; slow", gpu_vram: "optional 2-4 GB", model_size: "deepseek-r1:1.5b Q4; 1.1 GB", cpu_only: yes, low_resource: medium, cluster: high, recommendation: "server; lock Ollama digest"}
  SPG: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "<0.5 GB", cpu: "1 core", gpu_vram: "not required", model_size: "structural transformation; no ML model", cpu_only: yes, low_resource: high, cluster: high, recommendation: "local CPU structural baseline"}
  CPJ: {local_mechanism: "official repository unavailable", docker: unknown, ram: unknown, cpu: unknown, gpu_vram: unknown, model_size: unknown, cpu_only: unknown, low_resource: "not assessable", cluster: "not assessable", recommendation: "exclude until official versioned source is available"}
  DAMG: {local_mechanism: "official repository unavailable", docker: unknown, ram: unknown, cpu: unknown, gpu_vram: unknown, model_size: unknown, cpu_only: unknown, low_resource: "not assessable", cluster: "not assessable", recommendation: "exclude until official versioned source is available"}
  SARIM: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "0.5-1.5 GB", cpu: "1-2 cores", gpu_vram: "not required", model_size: "SVM joblib; 0.42 MB", cpu_only: yes, low_resource: high, cluster: high, recommendation: "local CPU; retain failure/degeneracy analysis"}
  ARIR: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "2-4 GB", cpu: "4+ cores; quadratic in pairs", gpu_vram: "official image CPU/OpenVINO", model_size: "RoBERTa-large 1.42 GB FP32 / 357 MB INT8", cpu_only: yes, low_resource: medium, cluster: high, recommendation: "reproducible CPU server; local only for small corpora"}
  DRIG: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "4-8 GB", cpu: "4+ cores; slow", gpu_vram: "about 3-6 GB if officially enabled", model_size: "DialoGPT medium; 1.42 GB", cpu_only: yes, low_resource: low, cluster: high, recommendation: "server; verify official GPU path"}
  TARGER-AM: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "4-8 GB", cpu: "2-4 cores", gpu_vram: "optional 3-5 GB", model_size: "shared TARGER IBM.h5; 1.23 GB", cpu_only: yes, low_resource: "low-medium", cluster: high, recommendation: "share server deployment with TARGER"}
  DSRM: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "4-8 GB", cpu: "4+ cores; very slow per pair", gpu_vram: "optional 2-4 GB", model_size: "deepseek-r1:1.5b Q4; 1.1 GB", cpu_only: yes, low_resource: low, cluster: high, recommendation: "GPU or strong CPU server; cap and record pairs"}
  DTERG: {local_mechanism: "oAMF repo + Docker Compose", docker: yes, ram: "2-4 GB", cpu: "4+ cores; OpenVINO", gpu_vram: "official image CPU/OpenVINO", model_size: "BART-large-MNLI 0.4B; 1.63 GB FP32 / 410 MB INT8", cpu_only: yes, low_resource: medium, cluster: high, recommendation: "reproducible CPU server; public endpoint for development"}
